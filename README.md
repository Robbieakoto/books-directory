# Awesome Authentication Built with TypeORM and Node.Js

Steps to run this project:

1. Run `npm i` command
2. Rename `ormconfig.example.json` to `ormconfig.json` and setup database settings inside of that file
3. Run `npm start` command
